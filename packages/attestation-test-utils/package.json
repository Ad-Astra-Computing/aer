{
  "name": "@aer-oss/attestation-test-utils",
  "version": "0.0.0",
  "private": true,
  "description": "Private crypto/JOSE helpers used only by the resource-node and mcp-guard test suites. Never published.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.8.0"
  }
}
